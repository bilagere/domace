---
import JournalOverview from "../../components/layout/JournalOverview.astro";

type JournalCategory = "ankommen" | "geniessen" | "ordnen" | "zusammen";

export function getStaticPaths() {
  return (["ankommen", "geniessen", "ordnen", "zusammen"] satisfies JournalCategory[]).map((category) => ({
    params: { category },
    props: { category },
  }));
}

interface Props { category: JournalCategory; }
const { category } = Astro.props;
---

<JournalOverview activeCategory={category} />
