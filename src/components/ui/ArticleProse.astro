<div class="article-prose"><slot /></div>

<style>
  .article-prose { max-width: var(--content-reading); }
  .article-prose :global(h2) { font-size: var(--text-xl); margin-top: var(--space-12); }
  .article-prose :global(h3) { font-size: var(--text-lg); margin-top: var(--space-8); }
  .article-prose :global(p) { font-size: var(--text-base); line-height: var(--leading-relaxed); margin-top: var(--space-4); }
  .article-prose :global(img) { height: auto; margin: var(--space-8) 0; object-fit: contain; width: 100%; }
  .article-prose :global(figure) { margin: var(--space-8) 0; }
  .article-prose :global(figure > img) { margin: 0; }
  .article-prose :global(figcaption) { color: var(--color-text-muted); font-size: var(--text-xs); margin-top: var(--space-2); }
  .article-prose :global(blockquote) { border-left: 2px solid var(--color-terracotta); font-family: var(--font-display); font-size: var(--text-xl); margin: var(--space-8) 0; padding-left: var(--space-5); }
</style>
