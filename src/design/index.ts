export const breakpoints = {
  compact: 640,
  medium: 768,
  wide: 1024,
} as const;

export const layout = {
  contentWidth: "78rem",
  readingWidth: "46rem",
  pageGutter: "clamp(1.25rem, 4vw, 4rem)",
} as const;

export const componentSizes = {
  controlHeight: "2.8rem",
  dividerWidth: "10rem",
} as const;
